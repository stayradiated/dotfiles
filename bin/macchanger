#!/usr/bin/env zsh

set -euxo pipefail

device_id='wlp0s20f3'

as-host "\
  sudo ip link set wlp0s20f3 down;\
  sudo macchanger --another --permanent --random --bia '${device_id}';\
  sudo ip link set wlp0s20f3 up;"
