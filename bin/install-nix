#!/usr/bin/env zsh

set -euo pipefail

export TMPDIR="$HOME/Downloads/nix"
mkdir -p "$TMPDIR"

curl \
  --proto '=https' --tlsv1.2 -sSf -L \
  https://install.determinate.systems/nix \
> "$TMPDIR/install-nix.sh"

chmod +x "$TMPDIR/install-nix.sh"

"$TMPDIR/install-nix.sh" install

rm -rf "$TMPDIR"
