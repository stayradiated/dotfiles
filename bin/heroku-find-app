#!/usr/bin/env zsh

set -euo pipefail

func heroku-app-name () {
  app_name=$1
  heroku apps --team runn --json | \
    grep "\"name\":\s*\"${app_name}\"" |\
    cut -d '"' -f 4
}

heroku-app-name $1
