snippet ir
  import * as React from 'react'

snippet rfc
options word
  interface Props {
  }

  const ${1}: React.FC<Props> = (props) => {
    const { ${3} } = props

    return (
      ${0}
    )
  }

  export default ${2}

snippet matcore
  import { ${1} } from '@material-ui/core'
