snippet ir
  import * as React from 'react'

snippet rfc
options word
  interface Props {
  }

  const ${1}: React.FC<Props> = (props) => {
    const { ${2} } = props

    return (
      ${0}
    )
  }

  export default $1

snippet func
  interface $1Options {
  }

  const ${1} = (options: $1Options) => {
    const { ${0} } = options
  }

snippet styles
  import styles from './`expand('%:p:t:r')`.module.css'
